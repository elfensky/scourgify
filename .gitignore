# Personal review maps — derived from YOUR Calibre library (tags/fandoms/characters/etc.).
# Generated locally by generate_maps.py / generate_followups.py; kept local, untracked.
*_map.csv
*_rebuild.csv
*_proposal.csv
*_fandom_aware.csv
_*.csv

# Bundled GENERIC defaults SHIP with the tool — keep them tracked.
!defaults/*.csv

# Calibre DB snapshots / backups
*.db

# Python / OS cruft
__pycache__/
*.pyc
.DS_Store

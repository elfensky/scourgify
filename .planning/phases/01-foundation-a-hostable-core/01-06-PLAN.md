---
phase: 01-foundation-a-hostable-core
plan: 06
type: execute
wave: 4
depends_on: ["01-04", "01-05"]
files_modified:
  - src/scourgify/common.py
  - src/scourgify/editlog.py
  - src/scourgify/wrangle.py
  - src/scourgify/classify.py
  - src/scourgify/staleness.py
  - src/scourgify/promote.py
  - src/scourgify/synopsis.py
  - tests/test_write_path.py
  - tests/test_editlog.py
  - tests/test_plan.py
  - tests/test_promote.py
  - tests/test_synopsis.py
autonomous: true
requirements: [FOUND-05]

estimate:
  tokens: 125000
  raw_tokens: 125000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "An op whose current value no longer matches its plan-time expected value is dropped from the change-set BEFORE the edit log is written and before anything is applied — it is never written to the book and it never gets an op line (FOUND-05, D-07, D-08)"
    - "The conflict verdict is editlog.conflict — one predicate shared by the apply-time check and by undo replay, so 'conflict' cannot come to mean two things (D-07)"
    - "The check runs on the near side of the calibre-debug subprocess, so ops.py and _writer.py are unchanged and the CLI needs no result plumbed back from the writer (D-07)"
    - "The run footer carries a skipped list of [book, field] pairs plus a count, the shared funnel's WriteResult (defined in plan 01-04) carries the same list, and the CLI prints one line per skipped book (D-08)"
    - "The all-skipped ordering is pinned and unambiguous: before-read -> conflict filter -> if NOTHING survives, write a header and footer with zero op lines and take NO snapshot and apply nothing; if something survives, snapshot -> log the surviving ops -> apply. A run that will write nothing must not cost a 527 MB-class snapshot, and an attempt that produced no write must still appear in History (REVIEW: Codex agreed concern 4, self-contradiction)"
    - "The wipe guard runs BEFORE the conflict filter, on the change-set the tool asked for — a runaway rule is refused whether or not drift happens to trim it"
    - "The conflict check needs no new read: editlog.before_values already runs on every write in both transports today, and _check_conflicts is a pure in-memory comparison over those already-read values (REVIEW: OpenCode plan-06 performance concern, rejected on this evidence)"
    - "Running the same ops list twice skips every op on the second run: the first run made current equal to after, which no longer equals the plan-time expected, so the second run writes no op line and changes no book (FOUND-05 idempotency)"
    - "Op lines are still written before the ops apply — the only ordering that survives a crash — and a line describing a write that never landed is read by undo as a conflict and skipped rather than clobbered (FOUND-05 concurrency, interrupted half)"
    - "op_set_field carries expected as a per-book mapping mirroring the shape of values; stamp_now, set_pref and create_column carry no expected value and are never conflict-checked (D-09)"
    - "Every tool that computes a write populates expected from the before-state ITS OWN write plan was computed against, and each producer's source is named individually rather than assumed uniform: wrangle from Plan.perbook (NOT Plan.before, which holds distinct-value SETS), staleness from the old value already carried in its change rows, promote from a third element backfill_plan() now returns, classify from the current_tags read at the head of apply_proposal, synopsis from the RAW comments text (D-09; REVIEW: Codex agreed concern 4, producer coverage)"
    - "Multi-valued-ness reaches the conflict check as an INJECTED reader, one per transport — column_is_multiple(con, field) for the CLI and a new common.field_is_multiple_via_api(api, field) for the plugin — matching the existing populated/read injection shape; editlog.conflict is never called with a guessed multi flag (REVIEW: Codex plan-06 MEDIUM)"
    - "common.column_values() reads the builtin comments field from the comments table, so a set_field op on comments has a real before-value instead of None for every book — without it every synopsis op would read as a conflict and the pass would write nothing"
  prohibitions:
    - "MUST NOT silently absorb a skipped or refused write: every conflict-skipped (book, field) is surfaced to the user BY BOOK — not merely counted, not only written to a log file the user has to go and read — and a run that skipped everything still writes a header and footer so the attempt is visible in History rather than disappearing"
    - "MUST NOT modify src/scourgify/ops.py or src/scourgify/_writer.py — the check lives on the near side of the calibre-debug subprocess, and ops.py must keep importing nothing from scourgify and nothing from Calibre at import time"
    - "MUST NOT let ops.py or _writer.py import rich, ui, wizard or report — they run under calibre-debug, whose bundled Python has an empty site-packages"
    - "MUST NOT write a second equality test for the conflict verdict anywhere; editlog.conflict is the one predicate shared by the apply-time check and undo"
    - "MUST NOT populate expected from a fresh read taken at write time — that makes expected equal to current by construction and disables the whole check"
    - "MUST NOT raise SystemExit from _check_conflicts or from any producer's op construction; every one of them is job-reachable"
  artifacts:
    - src/scourgify/common.py
    - src/scourgify/editlog.py
    - tests/test_write_path.py
    - tests/test_editlog.py
    - tests/test_plan.py
  key_links:
    - "the shared pre-write protocol -> editlog.before_values -> editlog.conflict(current, expected, multi) -> the filtered ops handed to editlog.start and ops.apply_ops"
    - "the shared pre-write protocol -> the injected is_multi reader (common.column_is_multiple / common.field_is_multiple_via_api) -> editlog.conflict's multi argument"
    - "wrangle.Plan / classify apply / staleness / promote.backfill / synopsis -> common.op_set_field(expected=)"
    - "editlog.finish(rec, outcome, skipped=) -> the run footer History reads in phase 4"
    - "common.column_values('comments') -> the comments table -> synopsis's expected"
---

<objective>
Make the one shared pre-write protocol refuse to overwrite a value that changed since the plan was
computed: skip that `(book, field)`, report it by book, and log the run honestly.

Purpose: every plan in this tool is computed against a read, then applied later — sometimes minutes
later, sometimes after the user edited the book in Calibre in between. Today the write applies
regardless, so the user's more recent edit is silently lost. `editlog.conflict` already exists as
the pure, tested predicate for exactly this question, because undo needs the same verdict; this
plan puts it on the apply side too, in the ONE funnel plan 01-04 built, so the two can never come to
mean different things.

Output: an `expected` mapping on the write op, a conflict filter in the shared funnel, a `skipped`
list in the run footer and in the funnel's returned result, CLI output naming each skipped book, and
every producer populating `expected` from the before-state its plan was computed against.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/01-foundation-a-hostable-core/01-CONTEXT.md
@.planning/phases/01-foundation-a-hostable-core/01-PATTERNS.md
@CLAUDE.md
</context>

<flagged_planner_assumptions>
No `unclassified` edge-probe row is assigned to this plan. Both FOUND-05 rows (idempotency,
concurrency) were classified and are authored as truths above.

**Stated planner assumption on the op key shape (research Open Question 3, no user input taken):**
`expected` mirrors the existing `values` shape exactly — `{book_id: expected_value}` on the
`set_field` op, alongside `values`. Rationale: every consumer that already reads `values` needs
near-identical handling, and symmetry removes the chance of the two dicts drifting in key type
(string from JSON, int in process) the way `ops.coerce`'s own docstring warns about for `values`
alone. This matches the orchestrator's resolved recommendation.
</flagged_planner_assumptions>

<review_dispositions>
Disposition of every current actionable finding in `01-REVIEWS.md` that names this plan. Every claim
below was checked against the source before being acted on; the grounding notes record what was
found.

| Finding | Reviewer | Disposition | Where |
|---------|----------|-------------|-------|
| The plan contradicts itself on all-skipped runs: the filter runs before `backup_db()`, yet an all-skipped run is said to still write a header/footer | Codex (agreed 4, HIGH) | **incorporated** | Task 1 pins Codex's own recommended order verbatim, with the all-skipped branch taking NO snapshot and still writing header + footer. `must_haves.truths` states it too |
| `_write_run` "yields skipped to caller" conflicts with existing return shapes; define a small result object now | Codex (agreed 4 / plan-06 HIGH) | **incorporated, moved earlier** | `common.WriteResult` is now defined in plan 01-04 Task 1 with `skipped` already on it; this plan fills the field instead of inventing plumbing. Grounding: `write_ops` returns `None` today and `run_writer` prints and exits |
| `wrangle.Plan.before` holds distinct-value SETS, not per-book state | Codex (agreed 4, HIGH) | **incorporated — the plan text was wrong and is corrected** | Verified: `wrangle.py:328` is `self.before = {k: set() for k in self.cols}`; per-book state is `self.perbook[b][k]` (`wrangle.py:323`, `:336`). Task 3 now names `perbook` and the `self.cols` key→label mapping explicitly |
| `promote.backfill_plan()` returns only final tags and adds, not the original set | Codex (agreed 4, HIGH) | **incorporated** | Verified at `promote.py:314-330`: `cur` is read inside and discarded. Task 3 changes the return to a 3-tuple `(chg, adds, before)`. Only two call sites exist (`promote.py:357`, `wizard.py:75` which uses `[0]`), so the widening is safe |
| The classify proposal schema has no before-state column, so "proposal rows record the tags the plan was computed against" is false | Codex (agreed 4, MEDIUM) | **plan text corrected; schema change REJECTED with rationale** | Verified: `artifacts.PROP_COLS = ["book_id", "title", "added_tags", "proposed_new"]`. Task 3 no longer claims otherwise. Rejection rationale in Task 3: classify's WRITE plan is computed in `apply_proposal` from `cur = current_tags(con)`, not in the LLM pass; and a proposal-time `expected` would skip any book edited in between while `op_stamp_now("#wrangled", processed)` still stamps it (stamps carry no `expected`), permanently retiring that book from the backlog untagged |
| Conflict filtering needs field multiplicity; `write_ops` has no API-side equivalent of `column_is_multiple()` | Codex (agreed 4, MEDIUM) | **incorporated** | Verified: `common.column_is_multiple` is sqlite-only (`common.py:425`); the API-side expression exists only inside `ops.py:32`. Task 2 adds `common.field_is_multiple_via_api(api, field)` beside `values_via_api`/`populated_via_api` — mirroring that expression rather than importing from `ops.py`, which stays untouched |
| Add a separate task before the producer updates to extend artifact schemas where needed | Codex (suggestion) | **incorporated, re-scoped** | A new Task 2 lands the READ-side prerequisites — the API multiplicity reader and the `comments` branch in `column_values` — before producers touch `expected`. Extending the classify proposal schema is rejected above |
| Verify `wrangle.Plan.before` stores values in a format compatible with `editlog.conflict` | OpenCode (HIGH) | **incorporated** | Task 3 requires the expected value to be the same shape the transport's before-read returns (`sorted` list for a multi column), and `editlog.conflict` compares multi as sets so order carries no meaning |
| Add a test that applying the same ops twice skips everything | OpenCode (suggestion) | **already covered** | `test_applying_the_same_ops_list_twice_skips_everything_the_second_time` (Task 1) |
| Document that `stamp_now`/`set_pref`/`create_column` are not conflict-checked | OpenCode (MEDIUM) | **incorporated** | D-09 already excludes them; Task 3 now states the ONE consequence that matters — a conflict-skipped classify book is still stamped — and names how the user sees and recovers it |
| Conflict checking adds a before-read per write; verify the cost is acceptable | OpenCode (MEDIUM) | **rejected on source evidence** | No read is added. `editlog.before_values(read, ops)` already runs on every write in BOTH transports today (`common.py` `write_ops` and `run_writer`), because the edit log needs before-values regardless. `_check_conflicts` is a pure in-memory comparison over those already-read values. Recorded as a `must_haves` truth so a later phase does not re-raise it |
| `ops.py` and `_writer.py` must stay unchanged | Codex (agreed strength) | **already covered, now a prohibition** | Plus `git diff --stat` acceptance criteria |

**Finding produced while grounding this revision, not from either reviewer, and load-bearing:**
`common.column_values(con, "comments")` returns `{}` today. It routes through
`read_custom_column(con, "comments")`, which calls `custom_column_id(con, "comments")` — and
`comments` is a Calibre BUILTIN table, not a custom column, so the lookup returns None and the whole
read yields nothing. Every `set_field` op on `comments` therefore has a before-value of `None` for
every book in the CLI transport. With a conflict check in place and synopsis populating `expected`
from the actual blurb, every synopsis op would compare a real string against `None`, read as a
conflict, and be skipped — the pass would silently write nothing, forever. Task 2 fixes the read.
</review_dispositions>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: expected on the write op, the conflict filter in the shared funnel, and the skipped report</name>
  <reversibility rating="costly">D-08: tests/test_editlog.py pins the record shape and phase 4's History will read the footer's skipped field, so changing the reporting shape later means a coordinated change across the log writer, the CLI output, the tests and the History reader.</reversibility>
  <files>src/scourgify/common.py, src/scourgify/editlog.py, tests/test_editlog.py, tests/test_write_path.py</files>
  <read_first>
    - src/scourgify/common.py (as modified by plan 01-04: `_write_run` with its captured library path and injected readers, the `WriteResult` dataclass whose `skipped` field this plan fills, `write_ops`, `run_writer`; plus op_set_field at line ~314 and the op-constructor comment above it explaining that the ops-JSON shape is built ONLY there)
    - .planning/phases/01-foundation-a-hostable-core/01-04-PLAN.md (its Task 1 action — the pinned step order this plan inserts the conflict filter into, and `WriteResult`'s field list, which this plan must not change)
    - src/scourgify/editlog.py (whole file — conflict at ~135, before_values at ~68, records at ~83, start at ~113, finish at ~129, and the module docstring's rules on log-before-apply, on undo:false ops, and on what a missing footer means)
    - src/scourgify/ops.py (whole file — coerce's str-to-int book id normalization, and the docstring stating this module imports nothing from scourgify; it must stay unchanged)
    - src/scourgify/_writer.py (whole file — glue only; it must stay unchanged)
    - tests/test_editlog.py (the record-shape assertions and the conflict-predicate tests at ~239-250 — the shapes this task extends)
    - tests/test_write_path.py (the FakeApi / _lib / _pointed_at helpers and the shadow-replay test)
  </read_first>
  <behavior>
    - An op whose current value equals its expected value applies normally and gets an op line
    - An op whose current value differs from its expected value is dropped: no op line, no write, and it appears in the run's skipped list
    - An op with no expected mapping at all is applied unconditionally (unchanged behaviour for callers that have not been updated yet)
    - `stamp_now`, `set_pref` and `create_column` are never conflict-checked
    - A multi-value field compares as a set, a single-value field as a string, both by delegating to `editlog.conflict`
    - A run where every entry conflicts still writes a header and a footer, with zero op lines and a footer count equal to the number skipped
    - Re-applying an already-applied ops list skips everything
  </behavior>
  <action>
    Three edits, all on the near side of the subprocess boundary.

    (a) `common.op_set_field(field, values, expected=None)`. When `expected` is given, put it on the
    op dict under the key `expected`, stringifying book ids exactly as `values` already does, so the
    two dicts round-trip through JSON with matching key types. When it is None, omit the key
    entirely — an op without it is applied unconditionally, which keeps every not-yet-updated caller
    working. Update the constructor comment: the ops-JSON shape is still built only here.

    (b) `common._check_conflicts(ops, before, is_multi) -> tuple[list, list]` and its call in
    `_write_run`. It runs at the shared funnel's step between the before-read and the snapshot, so it
    lands ONCE for both transports. For each `set_field` op carrying `expected`, compare
    `before[field][int(book)]` against `expected[book]` with
    `editlog.conflict(current, expected, is_multi(field))` — do not write a second equality test
    anywhere. `is_multi` is an INJECTED reader supplied by the transport, exactly like `populated`
    and `read`; plan 01-04's `_write_run` gains the parameter and plan 01-06 Task 2 supplies both
    implementations. Do NOT guess multi-valued-ness from the value's Python type: a single-value
    column may legitimately hold a list-shaped read, and `column_is_multiple`'s own docstring says
    the storage shape proves nothing. Drop every conflicting `(book, field)` from that op's `values`
    (and from its `expected`), drop the op entirely if its `values` empties, and return the surviving
    ops plus a list of `[book, field]` pairs. Ops with no `expected` key pass through untouched.
    `stamp_now` / `set_pref` / `create_column` are skipped by the loop, never checked (D-09).

    **The exact order inside `_write_run`, pinned — this replaces the previous ambiguity about
    whether an all-skipped run snapshots.** (REVIEW: Codex agreed concern 4.)
    1. drop empty `set_field` ops; if none remain, `(nothing to write)` and a `WriteResult` with
       `outcome="noop"` — no lock work beyond release, no snapshot, no log
    2. acquire the write-run lock (plan 01-04)
    3. `check_wipe(ops, populated)` unless `force` — BEFORE the conflict filter, on the change-set
       the tool asked for. A runaway rule must be refused whether or not drift happens to trim it
       below the threshold; a guard that a race can disarm is not a guard
    4. `before = editlog.before_values(read, ops)`
    5. `ops, skipped = _check_conflicts(ops, before, is_multi)`
    6. **if `ops` is now empty (everything conflicted):** `rec = editlog.start(tool, [], before, …)`
       — a header and zero op lines — then `editlog.finish(rec, "skipped", skipped=skipped)`.
       Take NO snapshot and apply nothing, then return a `WriteResult` with `backup=None`,
       `outcome="skipped"` and the skipped list. A run that will write nothing must not cost a
       snapshot of a multi-hundred-megabyte metadata.db, and there is nothing to roll back TO;
       History still shows the attempt, which is the CONTEXT.md discretion note's actual intent
    7. **otherwise:** snapshot (`backup_db` + explicit prune, plan 01-04) →
       `editlog.start(tool, ops, before, …)` with only the SURVIVING ops → yield → apply →
       `editlog.finish(rec, outcome, skipped=skipped)`

    The filter therefore runs BEFORE `editlog.start` and before `backup_db` in both branches, so the
    log records only what will be attempted — the crash-safe "log before apply" rule is preserved
    exactly, and undo never sees a line for a write that did not happen (D-08).

    (c) Reporting. `editlog.finish(rec, outcome, skipped=None)` writes `skipped` (the list of
    `[book, field]` pairs) and its count `n_skipped` into the footer when non-empty. `_write_run`
    puts the same list on the `common.WriteResult` plan 01-04 defined — do NOT invent a second
    shape, and do not change `WriteResult`'s fields. Both transports report it: `write_ops` through
    its `out` callback, `run_writer` by printing one line per skipped book. Phase 2's diff-after
    reads the same field.

    `src/scourgify/ops.py` and `src/scourgify/_writer.py` are NOT touched. Confirm `ops.coerce`
    tolerates the extra `expected` key on the op dict (it should ignore it); if it does not, the fix
    is to make `coerce` ignore unknown keys, not to teach `apply_ops` about conflicts.

    Tests. In `tests/test_editlog.py` add:
    - `test_the_footer_carries_the_skipped_pairs_and_their_count`
    - `test_an_all_skipped_run_still_writes_a_header_and_a_footer_with_no_op_lines`
    In `tests/test_write_path.py` add:
    - `test_an_all_skipped_run_takes_no_snapshot` — every op conflicts; assert the backups directory
      gained no `ff_*.db`, the returned `WriteResult.backup` is None and its `outcome` is `skipped`,
      and the log still holds one header and one footer
    - `test_an_op_whose_value_drifted_is_skipped_not_clobbered` — set the library's value to
      something other than `expected`, run the write, assert the book's value is unchanged and the
      pair is in the skipped list
    - `test_a_matching_expected_value_applies_normally`
    - `test_an_op_without_an_expected_mapping_applies_unconditionally`
    - `test_stamp_and_pref_ops_are_never_conflict_checked`
    - `test_applying_the_same_ops_list_twice_skips_everything_the_second_time` (the idempotency truth)
    - `test_both_transports_skip_the_same_ops` — extend the existing shadow-replay pattern so the
      in-process and CLI paths produce the same surviving ops and the same skipped list
  </action>
  <verify>
    <automated>uv run tests/test_editlog.py</automated>
    <fails_when>non-zero exit, or "AssertionError" in the output</fails_when>
    <automated>uv run tests/test_write_path.py</automated>
    <fails_when>non-zero exit, or "AssertionError" in the output</fails_when>
    <automated>bash -ec 'for t in tests/test_*.py; do uv run "$t"; done'</automated>
    <fails_when>non-zero exit, or "Traceback" / "AssertionError" appears in the output</fails_when>
  </verify>
  <acceptance_criteria>
    - `uv run python -c "import sys,inspect; sys.path.insert(0,'src'); from scourgify import common, editlog; print('expected' in inspect.signature(common.op_set_field).parameters, 'skipped' in inspect.signature(editlog.finish).parameters)"` prints `True True`
    - `src/scourgify/common.py` contains `def _check_conflicts(` and `_write_run` calls it AFTER `editlog.before_values` and BEFORE any `backup_db` call, in both branches
    - `_check_conflicts` takes an `is_multi` callable parameter and never inspects a value's Python type to decide multi-valued-ness
    - `git diff --stat src/scourgify/ops.py src/scourgify/_writer.py` is empty
    - A write whose expected value no longer matches leaves the book's value unchanged, produces no op line for that pair, and names the book in the run's skipped list, on the returned `WriteResult.skipped`, and in the printed output
    - A run in which every entry conflicted still produces one `"kind": "run"` line and one `"kind": "end"` line, zero `"kind": "op"` lines, and NO new `ff_*.db` in the backups directory; its `WriteResult` has `backup=None` and `outcome="skipped"`
    - `common.WriteResult`'s field list is unchanged from plan 01-04 — this plan populates `skipped`, it does not add a field
    - Applying one ops list twice writes on the first pass and skips every entry on the second
    - `editlog.conflict` is the only equality test used for the verdict — no second comparison is written in `common.py`
  </acceptance_criteria>
  <done>A drifted value is skipped and named by book instead of overwritten, the footer records what was skipped, and both transports behave identically.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: The two read-side prerequisites the conflict check needs before any producer can be trusted</name>
  <files>src/scourgify/common.py, tests/test_write_path.py</files>
  <read_first>
    - src/scourgify/common.py lines 418-455 — `_populated_books`, `column_is_multiple`, `column_values`, `values_via_api`; note that `column_values` routes everything that is not `tags` through `read_custom_column`
    - src/scourgify/common.py lines 239-256 — `custom_column_id` (a `custom_columns` lookup) and `read_custom_column` (returns None when that lookup misses), which is WHY `comments` reads empty today
    - src/scourgify/ops.py lines 28-40 — `mult = bool(api.field_metadata.all_metadata().get(field, {}).get("is_multiple"))`, the API-side multiplicity expression this task mirrors in `common.py`. `ops.py` itself stays untouched
    - src/scourgify/synopsis.py lines 171-200 — `Plan.__init__`, specifically `self.blurbs = {b: strip_html(t) for b, t in con.execute("SELECT book, text FROM comments")}`; the STRIPPED text is not what a `comments` before-read returns
    - tests/test_write_path.py lines 15-45 — `FakeMeta.all_metadata()` and `FakeApi`, which already model `is_multiple`
  </read_first>
  <behavior>
    - `common.field_is_multiple_via_api(api, "tags")` is True; for a field whose `all_metadata()` entry has `is_multiple` set it is True; otherwise False
    - `common.column_values(con, "comments", books)` returns the book's stored description text, not None
    - `common.column_values(con, "comments")` with no books returns the whole `comments` table as `{book: text}`
    - A book with no row in `comments` maps to None — a real before-state ("nothing"), matching the existing contract for a custom column with no value
    - `common.column_is_multiple(con, "comments")` is False, so `editlog.conflict` compares it as a string
  </behavior>
  <action>
    Two small, load-bearing read-side fixes in `src/scourgify/common.py`. Neither is optional: Task 3
    cannot populate `expected` correctly without both.

    (a) **The API-side multiplicity reader.** Add
    `field_is_multiple_via_api(api, field) -> bool` beside `values_via_api` and
    `populated_via_api`, following their docstring pattern ("the in-process twin of X"). Body:
    `tags` is multi by definition; otherwise
    `bool(api.field_metadata.all_metadata().get(field, {}).get("is_multiple"))`. That expression
    already exists inside `ops.py`, but `ops.py` must stay unchanged and imports nothing from
    scourgify, so this is a deliberate second copy in the module that owns the injected readers —
    note that in the docstring so a later reader does not "fix" it by importing across the boundary.
    `run_writer` injects `is_multi=lambda f: column_is_multiple(con, f)`; `write_ops` injects
    `is_multi=lambda f: field_is_multiple_via_api(api, f)`. (REVIEW: Codex plan-06 MEDIUM.)

    (b) **The `comments` before-read.** `column_values(con, field, books)` currently sends every
    non-`tags` field through `read_custom_column`, which resolves the label in the `custom_columns`
    table. `comments` is a Calibre BUILTIN with its own table, so that lookup misses,
    `read_custom_column` returns None, and `column_values` yields `{}` — the before-value for a
    `comments` op is None for every book today. Add a `comments` branch alongside the existing `tags`
    branch: `dict(con.execute("SELECT book, text FROM comments"))`, then apply the same
    `books`-narrowing the function already does, with a missing book mapping to None. Give
    `_populated_books` the same branch (`{b for (b,) in con.execute("SELECT book FROM comments WHERE text != ''")}`)
    so the coarse wipe guard stops being blind to the description column as well; the synopsis pass
    only ever adds descriptions, so the guard's verdict on existing runs cannot change direction.

    Tests in `tests/test_write_path.py` (the file that already owns `FakeApi` and the fixture-library
    helpers):
    - `test_field_is_multiple_via_api_matches_the_sqlite_answer` — for `tags`, for a multi custom
      column and for a single-value custom column, assert `field_is_multiple_via_api(api, f)` equals
      `column_is_multiple(con, f)` on the same fixture library. One meaning of "multi", two readers
    - `test_column_values_reads_the_builtin_comments_field` — plant descriptions on two books in a
      fixture library, assert `column_values(con, "comments", [b1, b2])` returns those exact strings,
      and assert a book with no comments row maps to None
    - `test_a_comments_op_gets_a_real_before_value` — build a `set_field` op on `comments` and assert
      `editlog.before_values(lambda f, bs: column_values(con, f, bs), ops)["comments"]` holds the
      stored text rather than None. This is the assertion that would have caught the synopsis
      write-nothing failure
  </action>
  <verify>
    <automated>uv run tests/test_write_path.py</automated>
    <fails_when>non-zero exit, or "AssertionError" in the output</fails_when>
    <automated>bash -ec 'for t in tests/test_*.py; do uv run "$t"; done'</automated>
    <fails_when>non-zero exit, or "Traceback" / "AssertionError" appears in the output</fails_when>
  </verify>
  <acceptance_criteria>
    - `src/scourgify/common.py` contains `def field_is_multiple_via_api(`
    - `common.field_is_multiple_via_api(api, f)` agrees with `common.column_is_multiple(con, f)` for `tags`, a multi custom column and a single-value custom column on the same fixture library
    - `common.column_values(con, "comments", books)` returns the stored description text for a book that has one, and None for a book that does not
    - `common.run_writer` injects `is_multi` built from `column_is_multiple`, and `common.write_ops` injects one built from `field_is_multiple_via_api`
    - `git diff --stat src/scourgify/ops.py src/scourgify/_writer.py` is empty
    - `bash -ec 'for t in tests/test_*.py; do uv run "$t"; done'` exits 0
  </acceptance_criteria>
  <done>Both transports can answer "is this field multi-valued" the same way, and a `comments` op finally has a real before-value — the two facts the conflict check silently depends on.</done>
</task>

<task type="auto">
  <name>Task 3: Every producer populates expected from the before-state its own write plan was computed against</name>
  <files>src/scourgify/wrangle.py, src/scourgify/classify.py, src/scourgify/staleness.py, src/scourgify/promote.py, src/scourgify/synopsis.py, tests/test_plan.py</files>
  <read_first>
    - src/scourgify/common.py (as modified by Tasks 1-2 — op_set_field's expected parameter, field_is_multiple_via_api, the comments branch)
    - src/scourgify/wrangle.py lines 314-425 — the `Plan` class in full. Note precisely: `self.before` (line 328) is `{column key: set()}`, a DISTINCT-VALUE set per column, and is NOT per-book state. The per-book before values are `self.perbook[b].get(k, [])` (line 334, where `d` is built and handed to `transform`), and `self.cols` maps the internal column key `k` to the label `lab` that `self.changes` is keyed by (line 341)
    - src/scourgify/classify.py lines 171-205 — `apply_proposal`: `cur = current_tags(con)` is read at the head, `chg[b] = sorted(cur.get(b, set()) | set(r["added_tags"]))`, and `op_stamp_now("#wrangled", processed)` stamps EVERY processed book
    - src/scourgify/artifacts.py line 50 — `PROP_COLS = ["book_id", "title", "added_tags", "proposed_new"]`; there is no before-state column, and this task does not add one
    - src/scourgify/staleness.py lines 45-80 — `plan`/`rows` are `(book, old, new, age)` tuples and `write()` builds its op from them; `old` IS the plan-time before value
    - src/scourgify/promote.py lines 314-340 for `backfill_plan` (note `cur = current_tags(con)` is read and then discarded) and lines 349-390 for `backfill`; plus `src/scourgify/wizard.py` line 75, the only other caller, which uses `backfill_plan()[0]`
    - src/scourgify/synopsis.py lines 171-200 for `Plan.__init__` (`self.blurbs` holds `strip_html(text)`, NOT the raw stored value) and lines 245-256 for the write
    - tests/test_plan.py (how wrangle's Plan is driven in tests — the harness the new assertions extend)
  </read_first>
  <action>
    Each tool passes, as `expected`, the before-state ITS OWN write plan was computed against, at the
    point where it constructs its ops. Do NOT take a fresh read to build `expected` — that makes
    expected equal to current by construction and disables the whole check. The source for that
    state is DIFFERENT in each producer, so each is specified separately rather than by one rule.

    - **`wrangle.Plan.write`** — build `expected` from `self.perbook`, NOT from `self.before`.
      `self.before` is `{column key: set of distinct values}` for the library-wide audit report; it
      holds no per-book state at all. For each `lab, ch` in `self.changes`, find the column key `k`
      with `self.cols[k] == lab` and build
      `{b: sorted(self.perbook[b].get(k, [])) for b in ch}` — sorted, so the shape matches what the
      transport's before-read returns for a multi-valued column, and `restrict()` narrows it for free
      because it narrows `self.changes`. `Plan.__init__` already captures `perbook` at compute time,
      so this is genuinely plan-time state. (REVIEW: Codex agreed concern 4 — the previous plan text
      named `Plan.before` and was wrong.)
    - **`staleness.write(status_label, rows)`** — `rows` are `(book, old, new, age)`, so
      `expected = {b: o for b, o, n, _ in rows}` alongside the existing
      `values = {b: n for b, o, n, _ in rows}`. `old` was read when the age-derived change-set was
      computed; this producer needs no new state.
    - **`promote.backfill_plan()`** — widen the return from `(chg, adds)` to `(chg, adds, before)`
      where `before = {b: sorted(cur.get(b, set())) for b in adds}`, built from the `cur =
      current_tags(con)` the function already reads and currently discards. Update
      `promote.backfill` to unpack three. `wizard.py:75` uses `backfill_plan()[0]` and needs no
      change — check it still reads correctly rather than assuming. When `backfill_step` narrows the
      accepted books, build the op's `expected` from the accepted keys only.
      (REVIEW: Codex agreed concern 4.)
    - **`classify.apply_proposal`** — `expected = {b: sorted(cur.get(b, set())) for b in chg}`, from
      the `cur = current_tags(con)` read at the head of the function. State the reasoning in a
      comment, because it is the one producer where the obvious answer is wrong:
      **why not the proposal rows?** The proposal schema (`artifacts.PROP_COLS`) has no before-state
      column, and adding one is deliberately REJECTED here. Two reasons. First, the LLM pass produces
      a *suggestion*; the WRITE plan — the `cur | added_tags` union — is computed inside
      `apply_proposal`, so `cur` is that plan's before-state by definition. Second, a proposal-time
      `expected` would skip any book whose tags changed at all in between, while
      `op_stamp_now("#wrangled", processed)` still stamps it (stamps carry no `expected` by D-09) —
      the book would be permanently retired from the `--unclassified` backlog having never been
      tagged. The window this producer protects is therefore read→write, not proposal→write, and
      that is stated rather than glossed. (REVIEW: Codex agreed concern 4, classify proposal schema.)
    - **`synopsis`** — `expected` is the RAW description each book held when the pass judged it, and
      `Plan.__init__` does not currently keep it: `self.blurbs` holds `strip_html(text)`. Add
      `self.raw_blurbs = {b: t for b, t in con.execute("SELECT book, text FROM comments")}` in the
      same pass that builds `self.blurbs` (one query, reused), and build
      `expected = {b: self.raw_blurbs.get(b) for b in made}` for the `comments` op. Using the
      stripped text would make every op compare a stripped string against the stored HTML and read
      as a conflict, so the pass would write nothing. The `#synopsized` stamp carries no `expected`.
    - `stamp_now` ops (`#wrangled`, `#synopsized`) and `set_pref` / `create_column` carry no
      `expected` (D-09).

    Keep the payload small: only the fields and books the op actually writes.

    **The one stated consequence of stamps not being conflict-checked.** A classify or synopsis book
    whose value drifted is skipped, yet its stamp still applies, so it leaves the backlog untagged /
    unsettled. That is accepted for this phase rather than hidden: the skipped list names the book in
    the footer, on the `WriteResult`, and in the CLI output, and phase 2's diff-after turns it into a
    retry. Write it into `apply_proposal`'s and synopsis's comments so the next reader does not
    rediscover it as a bug.

    Tests: extend the existing per-tool test files rather than adding a new one. For `wrangle` (via
    `tests/test_plan.py`'s harness) AND for staleness, assert (a) the constructed op carries an
    `expected` entry for every book in `values`, (b) the expected values equal the before-state the
    plan was computed against rather than a fresh read — for wrangle, assert explicitly that the
    values come from `perbook` and are per-book, not the distinct-value sets in `Plan.before` — and
    (c) mutating one book's value between plan and write causes exactly that book to be skipped while
    the others apply. Add `test_backfill_plan_returns_the_before_tag_set` for promote and
    `test_synopsis_expected_is_the_raw_stored_description_not_the_stripped_one` for synopsis. Where a
    harness does not reach an end-to-end write, assert at the op-construction level and say so in the
    test's docstring.
  </action>
  <verify>
    <automated>uv run tests/test_plan.py</automated>
    <fails_when>non-zero exit, or "AssertionError" in the output</fails_when>
    <automated>uv run tests/test_write_path.py</automated>
    <fails_when>non-zero exit, or "AssertionError" in the output</fails_when>
    <automated>bash -ec 'for t in tests/test_*.py; do uv run "$t"; done'</automated>
    <fails_when>non-zero exit, or "Traceback" / "AssertionError" appears in the output</fails_when>
  </verify>
  <acceptance_criteria>
    - Every `set_field` op produced by wrangle, classify apply, staleness, promote backfill and synopsis carries an `expected` entry for every book in its `values`
    - `wrangle.Plan.write`'s body reads `self.perbook` to build `expected` and does NOT read `self.before`
    - `uv run python -c "import sys,inspect; sys.path.insert(0,'src'); from scourgify import promote; import ast; t=ast.parse(inspect.getsource(promote.backfill_plan)); print(any(isinstance(n, ast.Return) and isinstance(n.value, ast.Tuple) and len(n.value.elts)==3 for n in ast.walk(t)))"` prints `True`
    - `src/scourgify/wizard.py` line 75 still reads `promote.backfill_plan()[0]` and `uv run tests/test_wizard.py` exits 0
    - `synopsis.Plan` holds a raw-description mapping distinct from `self.blurbs`, and the `comments` op's `expected` is built from the raw one
    - No producer takes a fresh read of the field to populate `expected` — the values come from the state the plan was computed against
    - Mutating one book's value between plan and write skips exactly that book and applies the others, proved by wrangle's and staleness's tests
    - `stamp_now` ops (`#wrangled`, `#synopsized`) carry no `expected` key, and the accepted consequence (a skipped book is still stamped) is stated in a comment at both sites
    - `artifacts.PROP_COLS` is unchanged — no before-state column is added to the classify proposal
    - `bash -ec 'for t in tests/test_*.py; do uv run "$t"; done'` exits 0
  </acceptance_criteria>
  <done>Every write in the tool carries the state it was planned against — each producer from its own named source — so the conflict check has something real to check and a stale plan cannot overwrite a fresher edit.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| plan time -> apply time | The library can change between the read a plan was computed from and the write that applies it — by the user in Calibre, by FanFicFare, or by another scourgify run |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-05 | Tampering | the shared pre-write protocol's apply step | high | mitigate | `_check_conflicts` compares the fresh before-read against the op's plan-time `expected` with `editlog.conflict` and drops the drifted `(book, field)` before logging and before applying — skip and report, never overwrite |
| T-01-20 | Repudiation | a skipped write disappearing from the record | high | mitigate | The footer carries the skipped pairs and their count, the funnel returns them, both transports print them by book, and an all-skipped run still writes a header and footer so the attempt is in History |
| T-01-21 | Tampering | two different definitions of "conflict" between apply and undo | medium | mitigate | `editlog.conflict` is the single predicate; the plan forbids writing a second equality test in `common.py`, and the acceptance criteria assert it |
| T-01-22 | Tampering | an op line describing a write that never landed | low | accept | Accepted by design and unchanged: op lines are written before the apply because that is the only ordering that survives a crash, and undo is conflict-aware, so a never-landed op reads as a conflict and is skipped rather than clobbered |
| T-01-28 | Denial of Service | a false-positive conflict on every op, silently disabling a whole pass | high | mitigate | Two concrete causes are closed in Task 2 rather than left latent: `column_values()` returned `{}` for the builtin `comments` field (so every synopsis op would have compared a real blurb against None and been skipped), and multi-valued-ness is now an injected reader per transport instead of a guess from the value's Python type. `test_a_comments_op_gets_a_real_before_value` and `test_field_is_multiple_via_api_matches_the_sqlite_answer` pin both |
| T-01-29 | Repudiation | a conflict-skipped book still receiving its `stamp_now`, retiring it from the backlog untagged | medium | accept | `stamp_now` carries no `expected` by D-09, so it applies regardless. Accepted for this phase and made visible rather than hidden: the book is named in the footer's `skipped` list, on the `WriteResult`, and in the CLI output, and the comment at both stamp sites states it. Phase 2's diff-after turns it into a retry verb |
| T-01-SC | Tampering | package-manager installs | n/a | accept | No package is added or upgraded; the predicate and both readers already exist in this repository |
</threat_model>

<verification>
- `uv run tests/test_editlog.py` green with the footer-shape and all-skipped-run tests
- `uv run tests/test_write_path.py` green with the seven conflict tests, the three read-side tests and the extended shadow replay
- `uv run tests/test_plan.py` green with wrangle's `expected` assertions built from `perbook`
- `uv run tests/test_promote.py` and `uv run tests/test_synopsis.py` green with the widened `backfill_plan` return and the raw-description `expected`
- `uv run tests/test_wizard.py` green — `wizard.py:75`'s `backfill_plan()[0]` still reads correctly
- `git diff --stat src/scourgify/ops.py src/scourgify/_writer.py` empty
- `bash -ec 'for t in tests/test_*.py; do uv run "$t"; done'` green
</verification>

<success_criteria>
- An op whose current value no longer matches its expected value is skipped and reported by book, never overwritten
- The verdict comes from `editlog.conflict` and from nowhere else, with multi-valued-ness supplied by an injected per-transport reader
- The all-skipped ordering is unambiguous: header + footer, no snapshot, nothing applied
- The footer, the `WriteResult` and the CLI output all name the skipped books
- Every producer's `expected` comes from a named plan-time source, and a `comments` op has a real before-value
- `ops.py` and `_writer.py` are unchanged
</success_criteria>

<artifacts_this_phase_produces>
New symbols created by THIS plan (excluded from drift verification — they do not exist yet):

| Symbol | Kind | File |
|--------|------|------|
| `expected=` | keyword parameter on `common.op_set_field()` | `src/scourgify/common.py` |
| `expected` | key on the `set_field` op dict | ops-JSON shape, built in `src/scourgify/common.py` |
| `common._check_conflicts()` | function | `src/scourgify/common.py` |
| `common.field_is_multiple_via_api()` | function (the in-process twin of `column_is_multiple`) | `src/scourgify/common.py` |
| `is_multi=` | injected reader parameter on `common._write_run` | `src/scourgify/common.py` |
| `skipped=` | keyword parameter on `editlog.finish()` | `src/scourgify/editlog.py` |
| `skipped` / `n_skipped` | keys on the run-footer JSONL record | `src/scourgify/editlog.py` |
| `before` (3rd element) | widened return of `promote.backfill_plan()` | `src/scourgify/promote.py` |
| `raw_blurbs` | attribute on `synopsis.Plan` | `src/scourgify/synopsis.py` |
| `test_the_footer_carries_the_skipped_pairs_and_their_count` | test function | `tests/test_editlog.py` |
| `test_an_all_skipped_run_still_writes_a_header_and_a_footer_with_no_op_lines` | test function | `tests/test_editlog.py` |
| `test_an_all_skipped_run_takes_no_snapshot` | test function | `tests/test_write_path.py` |
| `test_field_is_multiple_via_api_matches_the_sqlite_answer` | test function | `tests/test_write_path.py` |
| `test_column_values_reads_the_builtin_comments_field` | test function | `tests/test_write_path.py` |
| `test_a_comments_op_gets_a_real_before_value` | test function | `tests/test_write_path.py` |
| `test_backfill_plan_returns_the_before_tag_set` | test function | `tests/test_promote.py` |
| `test_synopsis_expected_is_the_raw_stored_description_not_the_stripped_one` | test function | `tests/test_synopsis.py` |
| `test_an_op_whose_value_drifted_is_skipped_not_clobbered` | test function | `tests/test_write_path.py` |
| `test_a_matching_expected_value_applies_normally` | test function | `tests/test_write_path.py` |
| `test_an_op_without_an_expected_mapping_applies_unconditionally` | test function | `tests/test_write_path.py` |
| `test_stamp_and_pref_ops_are_never_conflict_checked` | test function | `tests/test_write_path.py` |
| `test_applying_the_same_ops_list_twice_skips_everything_the_second_time` | test function | `tests/test_write_path.py` |
| `test_both_transports_skip_the_same_ops` | test function | `tests/test_write_path.py` |

Behaviour changes to EXISTING symbols (not new): the shared pre-write protocol gains a conflict
filter between the before-read and the snapshot, and an all-skipped run now writes a header and
footer while taking no snapshot; `common.column_values()` and `common._populated_books()` gain a
`comments` branch; `promote.backfill_plan()` returns a 3-tuple; `synopsis.Plan` keeps the raw
descriptions alongside the stripped ones; `wrangle.Plan.write`, classify's apply, `staleness`,
`promote.backfill` and `synopsis` populate `expected` on the ops they construct.

Explicitly NOT changed by this plan: `artifacts.PROP_COLS` (no before-state column is added to the
classify proposal — see Task 3's rationale), `src/scourgify/ops.py`, `src/scourgify/_writer.py`, and
`common.WriteResult`'s field list (defined in plan 01-04; this plan only populates `skipped`).
</artifacts_this_phase_produces>

<output>
Create `.planning/phases/01-foundation-a-hostable-core/01-06-SUMMARY.md` when done
</output>

---
phase: 01-foundation-a-hostable-core
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/scourgify/common.py
  - tests/test_paths.py
  - tests/test_artifacts.py
  - tests/test_promote.py
  - tests/test_selection.py
  - tests/test_synopsis_queue.py
  - tests/test_write_path.py
  - tests/test_editlog.py
  - tests/test_restore_drill.py
  - tests/drive_wizard.py
autonomous: false
requirements: [FOUND-02, FOUND-03, XPLAT-01]

estimate:
  tokens: 105000
  raw_tokens: 105000
  tasks: 4
  confidence: low

must_haves:
  truths:
    - "Two throwaway libraries opened in sequence in ONE process never share an artifact path: artifacts.prop/rank/fail/syn_fail/review/ledger, common.backups_dir/rejects_path and editlog.log_path all resolve under user_dir()/data/<that library's uuid>/, and the two libraries' path sets are disjoint (D-01, FOUND-03)"
    - "Two library paths that resolve to the same library_id.uuid share ONE data/<uuid>/ tree — identity is the uuid, never the filesystem path; two libraries with different uuids share no path segment below data/ (FOUND-03 adjacency)"
    - "A library db with no library_id table resolves to data/nouuid-<sha1 of the library path, first 12 hex chars>/, and data_dir() with no resolvable library raises common.GuardrailError rather than returning a library-less path (FOUND-03 empty)"
    - "Opening library B then library A in one process yields the same data_dir() for each as opening A then B — the per-process uuid memo is keyed by library path, so open order changes nothing (FOUND-03 ordering)"
    - "user_dir() and every path below it are built with os.path.join, so the Windows home and its children form one tree rather than two differently-separated ones (XPLAT-01 adjacency)"
    - "On a host where os.name is nt but APPDATA and SCOURGIFY_HOME are both unset, user_dir() falls through to the XDG / home-config branch and returns a non-empty path rather than raising (XPLAT-01 empty)"
    - "Precedence is ONE total order on every OS: SCOURGIFY_HOME, then the Windows APPDATA branch when os.name is nt, then XDG_CONFIG_HOME/scourgify, then ~/.config/scourgify (XPLAT-01 ordering, D-05)"
    - "The legacy flat data/ tree is moved into data/<uuid>/ only when the uuid read from the newest data/backups/ff_*.db equals the resolving library's uuid; a data/MIGRATED marker records the move and stops it repeating (D-02, D-03)"
    - "With no legacy backup, or a legacy backup whose uuid differs from the resolving library's, every legacy file stays byte-identical where it is and the reason is printed (D-03 negative case)"
    - "scourgify rollback --list lists only the current library's snapshots, and the prune budget (BACKUP_KEEP / BACKUP_BUDGET / BACKUP_MIN) applies per library (D-04)"
    - "config.toml and overrides/ stay at the user_dir() root — they are not moved, copied or duplicated per library (D-01)"
  prohibitions:
    - "MUST NOT move, rename or delete any file in the user's existing data/ tree unless the owner proof matched (the uuid read from the newest legacy backup equals the resolving library's uuid); on a non-match, an unreadable legacy backup, or no legacy backup at all, the legacy tree is left byte-identical and the reason is printed — never a silent no-op and never a hopeful move"
  artifacts:
    - src/scourgify/common.py
    - tests/test_paths.py
  key_links:
    - "common.data_dir() -> common.library() -> common.ro_connect() -> common.library_uuid()"
    - "common.backups_dir() / common.rejects_path() / artifacts.*() / editlog.log_path() -> common.data_dir()"
    - "common.backup_db() -> common.backups_dir() -> common.data_dir()"
---

<objective>
Make the library uuid the primary key for every piece of scourgify's operational state, and make
`user_dir()` resolve on Windows.

Purpose: this is the tracer for "the unchanged core runs correctly when hosted". A plugin opens
whatever library the GUI has open and switches libraries mid-session; today every artifact path is
a pure function of `user_dir()` alone, so a throwaway library reads and writes the real library's
proposals, failures, edit log, backups, rejects and ledger. That bleed was observed once in plugin
phases 4-5. Every later plan in this phase (the write-run lock keyed by uuid, the per-library edit
log the conflict check writes) depends on this resolution existing.

Output: `common.data_dir()` resolving under `data/<library uuid>/`, a Windows branch in
`common.user_dir()`, a one-time guarded migration of the existing flat tree, and a contract test
that fails loudly if any future phase reintroduces a library-less artifact path.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-a-hostable-core/01-CONTEXT.md
@.planning/phases/01-foundation-a-hostable-core/01-PATTERNS.md
@CLAUDE.md
</context>

<assumption_delta_decision>
**Detector:** `kind: pluralization`, term "second", from "A second write job against the same library".

**The noun that is now primary — two of them, both promoted:**

1. **The library uuid is the primary key for all operational state.** The old single global
   `user_dir()/data/` tree is not a parallel path beside the new one; it is ONE VARIANT of the
   uuid-keyed tree — the current library's — and reaching it goes through the same
   `common.data_dir()` resolution as any other library. There is no "default" or "global" artifact
   path any more.
2. **The pre-write protocol is primary; `run_writer` and `write_ops` are two transports of it.**
   (Implemented in plan 01-04; recorded here because it is the same delta.) The sequence
   guard -> lock -> before-read -> conflict check -> backup -> log -> apply is the thing; the
   subprocess vs in-process apply is a parameter of it.

**Decision: `promote`.**

**Rationale:** CONTEXT.md D-01 locks the uuid level into `data/<uuid>/` and D-02/D-03 lock a
migration of the existing tree into it, which is the definition of promoting the plural to primary
rather than adding a second path alongside. Keeping the flat tree as a co-equal "no library" path
would recreate exactly the bleed this phase exists to close: whichever library resolved first would
own it. `config.toml` and `overrides/` are deliberately NOT promoted (D-01) — they stay singular at
the `user_dir()` root, and CONTEXT.md records "per-library config.toml" as a deferred idea.

**Invariant test encoding the generalized intent (Task 1):**
`tests/test_paths.py::test_every_artifact_path_is_library_scoped` enumerates every artifact path
function in the codebase and asserts each one resolves under `data/<uuid>/` for BOTH of two
libraries opened in sequence in one process. A future phase that adds an artifact path built on
`user_dir()` directly, or that reintroduces a library-less default, goes red on that test rather
than shipping a second bleed.
</assumption_delta_decision>

<flagged_planner_assumptions>
The edge probe returned these rows as `unclassified` for this plan's requirements. Per the spec-less
fallback protocol they are NOT auto-resolved into `must_haves.truths` — they are surfaced here as
stated planner assumptions:

- **FOUND-02 (unclassified).** Assumed edge shape: the env-var precedence order and the
  APPDATA-unset case. Both are authored as XPLAT-01 ordering/empty truths above, so the assumption
  is that FOUND-02 needs no additional edge beyond those. If the verifier disagrees, the gap is a
  Windows-specific path-normalization case (drive letters, UNC paths) that this phase does not
  claim to cover.
</flagged_planner_assumptions>

<tasks>

<task type="tracer">
  <name>Task 1: End-to-end library-scoped state — one library's artifacts, resolved through the uuid</name>
  <reversibility rating="costly">D-01: every artifact path already funnels through common.data_dir(), so the production change is one function — but moving config.toml or overrides/ per library later would need a SECOND on-disk migration of user data. Rating carried from CONTEXT.md.</reversibility>
  <files>src/scourgify/common.py, tests/test_paths.py</files>
  <read_first>
    - src/scourgify/common.py (lines 1-60 for HERE/DEFAULTS/user_dir/data_dir/backups_dir/rejects_path and the load-bearing "Paths are FUNCTIONS" comment; lines 82-125 for set_library/library/ro_connect; lines 448-470 for library_uuid)
    - src/scourgify/artifacts.py (lines 1-50 — every artifact path function built on data_dir())
    - src/scourgify/editlog.py (lines 44-50 — log_path()'s lazy data_dir import)
    - tests/test_paths.py (whole file — the assertions being repointed and the `env` contextmanager idiom)
    - tests/fixture_db.py (whole file — build(..., uuid=) fills library_id)
    - .planning/phases/01-foundation-a-hostable-core/01-PATTERNS.md (section "common.data_dir() -> uuid-aware")
  </read_first>
  <action>
    Wire ONE path end to end: library identity -> sqlite read -> path function -> every artifact
    consumer. No other call site changes.

    In `src/scourgify/common.py`:
    - Add a Windows branch to `user_dir()`. The total order is: `SCOURGIFY_HOME`; then, when
      `os.name == "nt"` and `APPDATA` is set and non-empty, `os.path.join(os.environ["APPDATA"], "scourgify")`;
      then `XDG_CONFIG_HOME`/scourgify; then `~/.config/scourgify`. Read `APPDATA` with
      `os.environ.get`, never subscript — an nt host without it must fall through, not raise
      (D-05, FOUND-02). Update the docstring: it currently says "mac + Linux only — no Windows".
    - Add a module-level `_UUID_CACHE: dict[str, str] = {}` (library path -> resolved uuid,
      memoized for the process) and a private `_resolve_uuid() -> str`. It calls `library()` (which
      already raises `GuardrailError` when nothing is resolvable), opens a short-lived `ro_connect()`,
      calls the EXISTING `library_uuid(con)`, closes it in a `finally`, and stores
      `u or f"nouuid-{hashlib.sha1(lib.encode()).hexdigest()[:12]}"`. Import `hashlib` at module
      top. Do NOT add a second uuid-reading query anywhere — `library_uuid` is the one reader.
    - Change `data_dir()` to `os.path.join(user_dir(), "data", _resolve_uuid())`. Keep it a
      FUNCTION with no import-time memo of `user_dir()` — `SCOURGIFY_HOME` set after import must
      still redirect the whole tree. Update its docstring to name the uuid level and the
      GuardrailError case.
    - Add a public `clear_uuid_cache() -> None` that empties `_UUID_CACHE`, mirroring
      `classify.clear_caches()`. Tests that point at a second library in one process need it; it is
      the supported way to reset, instead of poking the module global.
    - Do NOT touch `backups_dir()`, `rejects_path()`, `artifacts.py` or `editlog.log_path()` — they
      are already functions over `data_dir()` and inherit the uuid level for free. That inheritance
      is the point of the design and Task 1's test proves it.

    In `tests/test_paths.py`:
    - Repoint this file's own assertions that hardcode the flat tree
      (`test_data_paths_follow_scourgify_home_after_import`, `test_applied_proposal_archives_found_by_owner`,
      `test_backups_size_counts_and_sums`, `test_backups_size_empty`) to build a fixture library with
      `fixture_db.build(..., uuid=...)`, set `CALIBRE_LIBRARY` alongside `SCOURGIFY_HOME`, and assert
      against `common.data_dir()` rather than a literal path.
    - Add `test_every_artifact_path_is_library_scoped` — THE contract test named in the
      assumption-delta decision. Build two fixture libraries with distinct uuids in one tempdir,
      resolve them in sequence in one process (calling `clear_uuid_cache()` between), and for each
      collect every artifact path the codebase writes: `artifacts.prop()`, `artifacts.rank()`,
      `artifacts.fail()`, `artifacts.syn_fail()`, `artifacts.review()`, `artifacts.ledger()`,
      `common.backups_dir()`, `common.rejects_path()`, `editlog.log_path()`. Assert every path starts
      with that library's `data_dir()`, and that the two libraries' path sets are disjoint. This is
      the reproduction of the plugin phase-4/5 bleed (a 6-book throwaway library reporting a real
      library's applied-proposal archive).
    - Add `test_same_uuid_two_paths_share_one_tree` (adjacency): two library folders whose dbs carry
      the SAME uuid resolve to the same `data_dir()`.
    - Add `test_library_without_library_id_table_gets_a_nouuid_key` (empty): a db built with
      `uuid=None` resolves to a `data/nouuid-...` segment.
    - Add `test_data_dir_without_a_library_raises_guardrail` (empty): with `CALIBRE_LIBRARY` unset
      and `set_library(None)`, `common.data_dir()` raises `common.GuardrailError`.
    - Add `test_open_order_does_not_change_either_libraries_paths` (ordering): resolve A then B, and
      in a second pass B then A, and assert each library's `data_dir()` is identical across passes.
    - Add Windows-branch tests driving `user_dir()` with a monkeypatched `os.name` and an injected
      `APPDATA`: `SCOURGIFY_HOME` wins over APPDATA; APPDATA wins over `XDG_CONFIG_HOME` on nt;
      an nt host with APPDATA unset falls through to the XDG branch and returns a non-empty path;
      and every produced path is built with `os.sep` joins (assert no path contains a mixed
      separator run). Restore `os.name` in a `finally`.
  </action>
  <verify>
    <automated>uv run tests/test_paths.py</automated>
    <fails_when>non-zero exit, or "AssertionError" / "GuardrailError" appears in the output, or the trailing "N tests passed" line reports fewer than 12 tests</fails_when>
  </verify>
  <acceptance_criteria>
    - `src/scourgify/common.py` contains `def _resolve_uuid(` and `def clear_uuid_cache(`
    - `uv run python -c "import os,tempfile,sys; sys.path.insert(0,'src'); from scourgify import common; print(common.user_dir())"` prints a non-empty path
    - `uv run tests/test_paths.py` exits 0 and its summary line reports at least 12 tests
    - With two fixture libraries carrying distinct uuids, `common.data_dir()` for each shares no path segment below `data/`
    - `common.data_dir()` with no resolvable library raises `common.GuardrailError`, not `SystemExit` and not a bare path
    - `common.load_config()` and the overrides dir still resolve at the `user_dir()` root, unchanged
  </acceptance_criteria>
  <done>One library's every artifact path resolves under `data/<its uuid>/`, two libraries in one process are provably disjoint, `user_dir()` has a Windows branch, and `tests/test_paths.py` is green.</done>
</task>

<task type="auto">
  <name>Task 2: Repoint the eight remaining test files that assumed a library-less data tree</name>
  <files>tests/test_artifacts.py, tests/test_promote.py, tests/test_selection.py, tests/test_synopsis_queue.py, tests/test_write_path.py, tests/test_editlog.py, tests/test_restore_drill.py, tests/drive_wizard.py</files>
  <read_first>
    - src/scourgify/common.py (as modified by Task 1 — data_dir, clear_uuid_cache)
    - tests/test_artifacts.py (the SCOURGIFY_HOME-only harness at lines ~45-110)
    - tests/test_promote.py (line ~348, test_apply_decisions_counts_rows_it_could_not_decide)
    - tests/test_selection.py (line ~183, test_unclassified_defaults_own_the_invariant)
    - tests/test_synopsis_queue.py (line ~72, test_queue_defaults_read_the_failure_log)
    - tests/test_write_path.py (the `_lib` / `_pointed_at` helpers at lines ~180-200 and the three backups-glob assertions at lines ~257, 273, 284)
    - tests/test_editlog.py (line ~44, the hardcoded edits.jsonl path)
    - tests/test_restore_drill.py (line ~58, the hardcoded backups glob)
    - tests/drive_wizard.py (lines ~43-45, the hardcoded data dir and proposal path)
    - tests/fixture_db.py (build signature)
  </read_first>
  <action>
    Every one of these files exercised the old contract — a `data_dir()` that needed no library.
    Repoint each to the new one. Two mechanical shapes, pick per file:

    (a) Files that set `SCOURGIFY_HOME` but never `CALIBRE_LIBRARY`
    (`tests/test_artifacts.py`, `tests/test_promote.py`, `tests/test_selection.py`,
    `tests/test_synopsis_queue.py`): build a throwaway library with `fixture_db.build` into the same
    tempdir and set `CALIBRE_LIBRARY` alongside `SCOURGIFY_HOME` in the existing harness/contextmanager,
    so the whole file gets it once rather than test by test. Call `common.clear_uuid_cache()` on
    entry so a previous test's memo cannot leak in.

    (b) Files that already set `CALIBRE_LIBRARY` but assert against a hardcoded path
    (`tests/test_write_path.py`, `tests/test_editlog.py`, `tests/test_restore_drill.py`,
    `tests/drive_wizard.py`): replace the literal `os.path.join(home, "data", ...)` constructions
    with `common.backups_dir()` / `editlog.log_path()` / `common.data_dir()` calls made inside the
    same env block. Do not weaken an assertion into a recursive glob of the whole home — the point
    of each is still "a snapshot exists / does not exist at the resolved location".

    `tests/test_wizard_flow.py`, `tests/test_classify_run.py`, `tests/test_books_cli.py` and
    `tests/test_synopsis.py` already set `CALIBRE_LIBRARY` AND call `common.data_dir()` rather than a
    literal — leave them untouched. Success criterion 4 promises `tests/test_wizard_flow.py` passes
    unchanged; that promise is kept by not editing it, and Task 2's verify proves it still passes.

    `tests/drive_wizard.py` is not collected by CI (no `test_` prefix) but is the pre-release PTY
    check — repoint it in the same pass so it is not left broken.
  </action>
  <verify>
    <automated>bash -ec 'for t in tests/test_*.py; do uv run "$t"; done'</automated>
    <fails_when>non-zero exit, or "Traceback" / "AssertionError" appears in the output</fails_when>
  </verify>
  <acceptance_criteria>
    - Every `tests/test_*.py` file passes under the CI loop with no file skipped
    - `git diff --stat tests/test_wizard_flow.py` is empty (the file is not edited)
    - No test file still contains a literal `os.path.join(<home>, "data", ...)` construction for an artifact path; the assertion goes through `common.data_dir()`, `common.backups_dir()` or `editlog.log_path()`
    - `uv run tests/test_write_path.py` exits 0, including `test_write_ops_guards_snapshots_then_applies` finding its snapshot under the resolved backups dir
  </acceptance_criteria>
  <done>The full local suite is green against the uuid-keyed tree, and `tests/test_wizard_flow.py` and `tests/test_cli.py` are byte-unchanged.</done>
</task>

<task type="auto">
  <name>Task 3: One-time guarded migration of the legacy flat data/ tree, with the owner proof</name>
  <reversibility rating="costly">D-02: the move is a rename, so it is reversible by hand — but it runs once against a real 527 MB tree and leaves a marker, so an unproved move would put the user's history under the wrong library.</reversibility>
  <files>src/scourgify/common.py, tests/test_paths.py</files>
  <read_first>
    - src/scourgify/common.py (as modified by Task 1: user_dir, _resolve_uuid, data_dir; plus library_uuid at ~455 and backups_dir/rejects_path at ~47-55)
    - tests/test_paths.py (as modified by Task 1 — the two-library harness this task's tests reuse)
    - tests/fixture_db.py (build(..., uuid=) — the fixture that plants a legacy backup carrying a uuid)
    - .planning/phases/01-foundation-a-hostable-core/01-CONTEXT.md (D-02, D-03, D-04 and the "Specific Ideas" note on the real tree's shape)
  </read_first>
  <action>
    Add `migrate_legacy_data() -> str | None` to `src/scourgify/common.py`, called from
    `data_dir()` immediately before it returns (once per process — guard with a module-level
    "already attempted" flag so a hot path does not stat the tree on every artifact read).

    The rule, exactly as D-02/D-03 lock it:
    - Do nothing at all if `user_dir()/data/MIGRATED` exists, or if `data/<resolved uuid>/` already
      exists, or if there are no legacy entries sitting directly under `data/` (a legacy entry is a
      file, or the `backups` directory — a directory whose name is a uuid or starts with `nouuid-`
      is NOT legacy).
    - Owner proof: glob `data/backups/ff_*.db`, take the newest by mtime, open it with the same
      read-only sqlite URI shape `ro_connect()` uses, and read its uuid with the EXISTING
      `library_uuid()`. Move ONLY when that uuid equals the resolving library's uuid.
    - On a match: `os.makedirs(data/<uuid>)` then `os.rename` each legacy entry into it (a rename,
      never a copy — the real tree is ~527 MB), then write `data/MIGRATED` containing the uuid and a
      timestamp, and print one line naming what moved and where.
    - On no legacy backup, an unreadable legacy backup, or a uuid mismatch: move NOTHING, leave every
      legacy file byte-identical, and PRINT the reason (which uuid was found, which was expected).
      Return None. A throwaway library resolved first must never claim the real library's history.
    - Raise `common.GuardrailError` — never `SystemExit` — if the rename itself fails partway, naming
      what moved and what did not, so a job-reachable caller can catch it.

    Confirm `backups_dir()`, `_backup_path()`, `_prune_backups()` and `rollback_cmd()` need no edit:
    they are already functions over `data_dir()`, so BACKUP_KEEP / BACKUP_BUDGET / BACKUP_MIN and
    `rollback --list` become per library for free (D-04). Do not add a per-library parameter to any
    of them.

    In `tests/test_paths.py` add, mirroring the real tree's shape (archives plus a backup carrying
    the uuid):
    - `test_legacy_tree_moves_when_the_backup_proves_ownership` — plant
      `data/classify_proposal_applied_20260726-000000.csv`, `data/promote_ledger.csv` and
      `data/backups/ff_old.db` (a fixture db built with the library's uuid); resolve; assert the
      files now live under `data/<uuid>/`, `data/MIGRATED` exists, and nothing remains directly
      under `data/`.
    - `test_legacy_tree_is_untouched_when_the_backup_belongs_to_another_library` — the same tree,
      but the planted backup carries a DIFFERENT uuid; assert every legacy file is still at its
      original path with identical bytes, `data/MIGRATED` does not exist, and the printed reason
      names both uuids.
    - `test_legacy_tree_is_untouched_when_there_is_no_backup_to_prove_ownership` — legacy files, no
      `data/backups/` at all; assert nothing moved and the reason is printed.
    - `test_migration_does_not_repeat_once_the_marker_exists` — run resolution twice; assert the
      second pass moves nothing and prints nothing.
    - `test_config_and_overrides_stay_at_the_user_dir_root` — plant `config.toml` and
      `overrides/fandoms.csv` under `user_dir()`; after migration assert both are still exactly
      there and no copy appeared under `data/<uuid>/` (D-01).
  </action>
  <verify>
    <automated>uv run tests/test_paths.py</automated>
    <fails_when>non-zero exit, or "AssertionError" in the output, or the summary line reports fewer than 17 tests</fails_when>
  </verify>
  <acceptance_criteria>
    - `src/scourgify/common.py` contains `def migrate_legacy_data(`
    - A fixture legacy tree whose newest backup carries the resolving library's uuid ends up entirely under `data/<uuid>/`, with a `data/MIGRATED` marker present
    - A fixture legacy tree whose newest backup carries a different uuid is byte-identical after resolution, has no `data/MIGRATED` marker, and the run printed both uuids
    - A fixture legacy tree with no `data/backups/` directory is byte-identical after resolution and the run printed the reason
    - `config.toml` and `overrides/` are still directly under `user_dir()` after a successful migration, with no per-library copy
    - `bash -ec 'for t in tests/test_*.py; do uv run "$t"; done'` exits 0
  </acceptance_criteria>
  <done>The legacy tree migrates exactly once, only on a proved owner match, and the negative cases leave the user's data untouched with a stated reason.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Confirm the migration against the developer's real library</name>
  <precondition>CALIBRE_LIBRARY points at the real Calibre library and ~/.config/scourgify/data holds the existing flat tree (backups plus applied-proposal archives)</precondition>
  <files>src/scourgify/common.py</files>
  <what-built>
    A uuid-keyed artifact tree and a one-time guarded auto-move of the existing flat
    `~/.config/scourgify/data` tree into `data/&lt;library uuid&gt;/`, guarded by an owner proof that
    reads the uuid out of the newest legacy backup. The fixture tests prove the logic; this proves
    it against ~527 MB of the developer's own history — the one thing no test can stand in for and
    the one thing that cannot be un-lost.
  </what-built>
  <how-to-verify>
    Run these three read-only commands and report what they print.

    1. `uv run scourgify rollback --list`
       Expect: the backups path in the header now contains a uuid segment
       (`.../data/&lt;uuid&gt;/backups`), and the snapshots listed are the same ones that were
       previously under `.../data/backups`, at the same count.
    2. `ls ~/.config/scourgify/data`
       Expect: exactly one uuid-named directory plus a `MIGRATED` file. No loose `*.csv` and no
       loose `backups/` directory.
    3. `ls ~/.config/scourgify` and `cat ~/.config/scourgify/data/MIGRATED`
       Expect: `config.toml` and `overrides/` still directly under `~/.config/scourgify`, and the
       marker naming the library uuid the tree was moved under.

    STOP and report instead of approving if the snapshot count changed, if anything remains loose
    under `data/`, or if `config.toml` or `overrides/` moved.
  </how-to-verify>
  <acceptance_criteria>
    - `scourgify rollback --list` reports the same snapshot count as before this plan, now under a uuid-keyed backups path
    - Nothing remains directly under `~/.config/scourgify/data` except the uuid directory and the `MIGRATED` marker
    - `config.toml` and `overrides/` are still at `~/.config/scourgify`
  </acceptance_criteria>
  <resume-signal>Type "approved", or paste the three commands' output describing what does not match</resume-signal>
  <done>The developer has confirmed their real history moved intact under the library uuid and their configuration stayed global.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| library -> artifact tree | Which library's uuid a path resolves under decides which library's history a run reads and writes |
| legacy tree -> uuid tree | The one-time auto-move relocates the user's real data on the strength of a uuid read out of a backup file |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-01 | Information Disclosure | `common.data_dir()` and every artifact path built on it | high | mitigate | Uuid-keyed resolution (Task 1) plus `test_every_artifact_path_is_library_scoped`, which enumerates every artifact path function and asserts two libraries' sets are disjoint |
| T-01-02 | Tampering | `common.migrate_legacy_data()` | high | mitigate | D-03 owner proof: move only when the uuid read from the newest `data/backups/ff_*.db` equals the resolving library's uuid; the negative case is a test that asserts byte-identical legacy files and a printed reason |
| T-01-03 | Denial of Service | `common.migrate_legacy_data()` partial rename | medium | mitigate | A rename (not a copy) of ~527 MB, and a `GuardrailError` naming what moved and what did not if it fails partway — never a silent half-move, never a `SystemExit` a Calibre job would swallow |
| T-01-06 | Information Disclosure | `%APPDATA%\scourgify` / `~/.config/scourgify` permissions | low | accept | Desktop-local single-user tool; the location mirrors Calibre's own `%APPDATA%\calibre` convention and inherits the OS default ACL. No secret is stored here in this phase (keys live in the plugin's JSONConfig, phase 3) |
| T-01-SC | Tampering | package-manager installs | n/a | accept | No package is added, upgraded or newly imported by this plan — `hashlib` is stdlib. RESEARCH.md records the Package Legitimacy Audit as not applicable this phase and `pyproject.toml`'s only runtime dependency stays `rich>=13` |
</threat_model>

<verification>
- `uv run tests/test_paths.py` green with the two-library disjointness test, the Windows-branch tests, and the five migration tests
- `bash -ec 'for t in tests/test_*.py; do uv run "$t"; done'` green
- `git diff --stat tests/test_wizard_flow.py tests/test_cli.py` empty
- The Task 4 human check reports the real tree moved intact
</verification>

<success_criteria>
- `common.data_dir()` returns `user_dir()/data/<library uuid>/` and raises `GuardrailError` with no resolvable library
- `common.user_dir()` has a Windows branch honouring the locked precedence order
- The legacy tree migrates once, only on a proved owner match
- No artifact path anywhere in the codebase resolves outside `data/<uuid>/`, enforced by a contract test
</success_criteria>

<artifacts_this_phase_produces>
New symbols created by THIS plan (excluded from drift verification — they do not exist yet):

| Symbol | Kind | File |
|--------|------|------|
| `common._UUID_CACHE` | module-level dict | `src/scourgify/common.py` |
| `common._resolve_uuid()` | function | `src/scourgify/common.py` |
| `common.clear_uuid_cache()` | function | `src/scourgify/common.py` |
| `common.migrate_legacy_data()` | function | `src/scourgify/common.py` |
| `data/MIGRATED` | on-disk marker file | `user_dir()/data/MIGRATED` |
| `data/<uuid>/` | on-disk directory level | `user_dir()/data/<library uuid>/` |
| `nouuid-<sha1[:12]>` | fallback uuid key format | resolved by `common._resolve_uuid()` |
| `test_every_artifact_path_is_library_scoped` | test function | `tests/test_paths.py` |
| `test_same_uuid_two_paths_share_one_tree` | test function | `tests/test_paths.py` |
| `test_library_without_library_id_table_gets_a_nouuid_key` | test function | `tests/test_paths.py` |
| `test_data_dir_without_a_library_raises_guardrail` | test function | `tests/test_paths.py` |
| `test_open_order_does_not_change_either_libraries_paths` | test function | `tests/test_paths.py` |
| `test_legacy_tree_moves_when_the_backup_proves_ownership` | test function | `tests/test_paths.py` |
| `test_legacy_tree_is_untouched_when_the_backup_belongs_to_another_library` | test function | `tests/test_paths.py` |
| `test_legacy_tree_is_untouched_when_there_is_no_backup_to_prove_ownership` | test function | `tests/test_paths.py` |
| `test_migration_does_not_repeat_once_the_marker_exists` | test function | `tests/test_paths.py` |
| `test_config_and_overrides_stay_at_the_user_dir_root` | test function | `tests/test_paths.py` |

Behaviour changes to EXISTING symbols (not new): `common.user_dir()` gains a Windows branch;
`common.data_dir()` gains the uuid level; `common.backups_dir()`, `common.rejects_path()`,
`artifacts.*()` and `editlog.log_path()` inherit it without source changes.
</artifacts_this_phase_produces>

<output>
Create `.planning/phases/01-foundation-a-hostable-core/01-01-SUMMARY.md` when done
</output>

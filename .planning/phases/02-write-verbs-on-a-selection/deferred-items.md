# Deferred items — Phase 02

- **tests/test_defaults_resource.py::test_cost_estimate_is_identical_from_the_zip_and_from_the_package**
  fails on a clean `develop` checkout (confirmed via `git stash` before any 02-01 changes) —
  pre-existing, unrelated to write-verb plumbing. Out of scope for 02-01 per the scope-boundary
  rule; not auto-fixed.
